# Core harness (needed for the offline mock run):
numpy==2.5.2
matplotlib==3.11.1
rich==15.0.0
python-dotenv==1.2.2

# Live large-tier backends (install only the one you use):
# openai>=1.0        # openai_compatible: Groq, OpenAI, Together, OpenRouter, vLLM
# anthropic>=0.40    # anthropic: Claude
# boto3>=1.34        # bedrock: Claude/others on AWS
# Small tier uses Ollama's HTTP API directly (no python package required).
